---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: ASP.NET 웹 페이지(Razor)가 있는 차트에 데이터 표시 | 마이크로 소프트 문서
author: rick-anderson
description: 이 장에서는 차트에 데이터를 표시하는 방법을 설명합니다. 이전 장에서는 수동으로 그리드에 데이터를 표시하는 방법을 배웠습니다. 이 장에서는...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: ad55d4898ee39e0239ef8b0bd5eea6387af3c816
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542887"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="a2642-105">웹 페이지(Razor)를 ASP.NET 차트에 데이터 표시</span><span class="sxs-lookup"><span data-stu-id="a2642-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="a2642-106">[로 마이크로 소프트](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="a2642-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="a2642-107">이 문서에서는 도우미를 사용하여 차트를 사용하여 ASP.NET 웹 페이지(Razor) `Chart` 웹 사이트에 데이터를 표시하는 방법을 설명합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="a2642-108">**당신이 배울 것이다 무엇**:</span><span class="sxs-lookup"><span data-stu-id="a2642-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="a2642-109">차트에 데이터를 표시하는 방법.</span><span class="sxs-lookup"><span data-stu-id="a2642-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="a2642-110">기본 제공 테마를 사용하여 차트에 스타일을 정하는 방법.</span><span class="sxs-lookup"><span data-stu-id="a2642-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="a2642-111">차트를 저장하는 방법과 더 나은 성능을 위해 차트를 캐시하는 방법.</span><span class="sxs-lookup"><span data-stu-id="a2642-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="a2642-112">다음은 이 문서에서 소개한 ASP.NET 프로그래밍 기능입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="a2642-113">`Chart` 도우미입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="a2642-114">이 문서의 정보는 웹 페이지 1.0 및 웹 페이지 2ASP.NET 에 적용됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="a2642-115">차트 도우미</span><span class="sxs-lookup"><span data-stu-id="a2642-115">The Chart Helper</span></span>

<span data-ttu-id="a2642-116">데이터를 그래픽 형식으로 표시하려면 도우미를 사용할 `Chart` 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="a2642-117">도우미는 `Chart` 다양한 차트 유형의 데이터를 표시하는 이미지를 렌더링할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="a2642-118">서식 지정 및 레이블 지정에 대한 많은 옵션을 지원합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="a2642-119">도우미는 `Chart` Microsoft Excel또는 영역 차트, 막대 차트, 열 차트, 꺾은줄 차트 및 원형 차트와 &#8212; 기타 도구에서 잘 알고 있을 수 있는 모든 유형의 차트와 주식 차트와 같은 보다 전문적인 차트를 포함하여 30개 이상의 유형의 차트를 렌더링할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="a2642-120">**지역도** ![설명: 지역도표 유형 그림](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="a2642-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="a2642-121">**막대 차트** ![설명: 막대형 차트 유형 그림](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="a2642-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="a2642-122">**열체 차트** ![설명: 열수 차트 유형 그림](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="a2642-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="a2642-123">**표선 차트** ![설명: 선급선 차트 유형 그림](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="a2642-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="a2642-124">**원형 차트** ![설명: 원형 차트 유형 그림](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="a2642-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="a2642-125">**주식 차트** ![설명: 주식 차트 유형 의 그림](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="a2642-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="a2642-126">차트 요소</span><span class="sxs-lookup"><span data-stu-id="a2642-126">Chart Elements</span></span>

<span data-ttu-id="a2642-127">차트는 데이터와 범례, 축, 계열 등과 같은 추가 요소를 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="a2642-128">다음 그림은 `Chart` 도우미를 사용할 때 사용자 지정할 수 있는 많은 차트 요소를 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="a2642-129">이 문서에서는 이러한 요소중 일부(전부는 아님)를 설정하는 방법을 보여 주어집니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-129">This article shows you how to set some (not all) of these elements.</span></span>

![설명: 차트 요소를 보여주는 그림](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="a2642-131">데이터에서 차트 만들기</span><span class="sxs-lookup"><span data-stu-id="a2642-131">Creating a Chart from Data</span></span>

<span data-ttu-id="a2642-132">차트에 표시되는 데이터는 배열, 데이터베이스에서 반환된 결과 또는 XML 파일에 있는 데이터에서 나온 데이터일 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="a2642-133">배열 사용</span><span class="sxs-lookup"><span data-stu-id="a2642-133">Using an Array</span></span>

<span data-ttu-id="a2642-134">[Razor 구문을 사용하여 ASP.NET 웹 페이지 프로그래밍 소개에서](https://go.microsoft.com/fwlink/?LinkId=202890)설명한 대로 배열을 사용하면 유사한 항목의 컬렉션을 단일 변수에 저장할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="a2642-135">배열을 사용하여 차트에 포함할 데이터를 포함할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="a2642-136">이 절차에서는 기본 차트 유형을 사용하여 배열의 데이터에서 차트를 만드는 방법을 보여 주며 이 절차에서 차트를 만드는 방법을 보여 주시면 됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="a2642-137">또한 페이지 내에 차트를 표시하는 방법도 보여 주었습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="a2642-138">*ChartArrayBasic.cshtml이라는*새 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="a2642-139">기존 콘텐츠를 다음 내용으로 바꿉꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="a2642-140">코드는 먼저 새 차트를 만들고 너비와 높이를 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="a2642-141">메서드를 사용 하 여 `AddTitle` 차트 제목을 지정 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="a2642-142">데이터를 추가하려면 메서드를 `AddSeries` 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="a2642-143">이 `name`예제에서는 `xValue` `yValues` `AddSeries` 메서드의 " 및 매개 변수를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="a2642-144">`name` 매개 변수가 차트 범례에 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="a2642-145">`xValue` 매개 변수에는 차트의 가로 축을 따라 표시되는 데이터 배열이 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="a2642-146">`yValues` 매개변수에는 차트의 세로 점을 플롯하는 데 사용되는 데이터 배열이 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="a2642-147">메서드는 `Write` 실제로 차트를 렌더링합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="a2642-148">이 경우 차트 유형을 지정하지 않았기 때문에 `Chart` 도우미는 열차트인 기본 차트를 렌더링합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="a2642-149">브라우저에서 페이지를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-149">Run the page in the browser.</span></span> <span data-ttu-id="a2642-150">브라우저에 차트가 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="a2642-151">차트 데이터에 데이터베이스 쿼리 사용</span><span class="sxs-lookup"><span data-stu-id="a2642-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="a2642-152">차트로 만들 정보가 데이터베이스에 있는 경우 데이터베이스 쿼리를 실행한 다음 결과의 데이터를 사용하여 차트를 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="a2642-153">이 절차에서는 웹 페이지 사이트에서 데이터베이스 작업 소개 문서에서 만든 데이터베이스의 데이터를 읽고 표시하는 방법을 보여 [ASP.NET.](https://go.microsoft.com/fwlink/?LinkId=202893)</span><span class="sxs-lookup"><span data-stu-id="a2642-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="a2642-154">폴더가 아직 없는 경우 웹 사이트의 루트에 *앱\_데이터* 폴더를 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="a2642-155">*앱\_데이터* 폴더에 ASP.NET [웹 페이지 사이트의 데이터베이스 작업 소개에](https://go.microsoft.com/fwlink/?LinkId=202893)설명된 *SmallBakery.sdf라는* 데이터베이스 파일을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="a2642-156">*ChartDataQuery.cshtml이라는*새 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="a2642-157">기존 콘텐츠를 다음 내용으로 바꿉꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="a2642-158">코드는 먼저 SmallBakery 데이터베이스를 열고 . `db`</span><span class="sxs-lookup"><span data-stu-id="a2642-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="a2642-159">이 변수는 `Database` 데이터베이스에서 읽고 쓰는 데 사용할 수 있는 개체를 나타냅니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="a2642-160">다음으로 코드는 SQL 쿼리를 실행하여 각 제품의 이름과 가격을 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="a2642-161">코드는 새 차트를 만들고 차트의 `DataBindTable` 메서드를 호출하여 데이터베이스 쿼리를 전달합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="a2642-162">이 메서드는 두 개의 `dataSource` 매개 변수를 사용합니다: 매개 `xField` 변수는 쿼리의 데이터에 대한 것이고 매개 변수를 사용하면 차트의 x축에 사용되는 데이터 열을 설정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="a2642-163">`DataBindTable` 메서드를 사용하는 대신 도우미의 메서드를 `AddSeries` `Chart` 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="a2642-164">메서드를 `AddSeries` 사용 하 `xValue` `yValues` 고 매개 변수를 설정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="a2642-165">예를 들어 다음과 같은 `DataBindTable` 메서드를 사용하는 대신 다음과 같이 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="a2642-166">다음과 `AddSeries` 같은 방법을 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="a2642-167">둘 다 동일한 결과를 렌더링합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-167">Both render the same results.</span></span> <span data-ttu-id="a2642-168">차트 `AddSeries` 형식과 데이터를 보다 명시적으로 지정할 수 있으므로 이 `DataBindTable` 방법은 더 유연하지만 추가 유연성이 필요하지 않은 경우 이 방법을 더 쉽게 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="a2642-169">브라우저에서 페이지를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="a2642-170">XML 데이터 사용</span><span class="sxs-lookup"><span data-stu-id="a2642-170">Using XML Data</span></span>

<span data-ttu-id="a2642-171">차트의 세 번째 옵션은 XML 파일을 차트의 데이터로 사용하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="a2642-172">이렇게 하려면 XML 파일에 XML 구조를 설명하는 스키마*파일(.xsd* 파일)도 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="a2642-173">이 절차에서는 XML 파일에서 데이터를 읽는 방법을 보여 주며, 이 절차에서는 데이터를 읽는 방법을 보여 주시면 됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="a2642-174">*\_앱 데이터* 폴더에서 *data.xml*이라는 새 XML 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="a2642-175">기존 XML을 가상의 회사의 직원에 대한 일부 XML 데이터인 다음으로 바꿉습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="a2642-176">*\_앱 데이터* 폴더에서 *data.xsd라는*새 XML 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="a2642-177">(이 시간 확장은 *.xsd입니다.)*</span><span class="sxs-lookup"><span data-stu-id="a2642-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="a2642-178">기존 XML을 다음으로 바꿉꿉을 다음과 같은 항목으로 바꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="a2642-179">웹 사이트의 루트에서 *ChartDataXML.cshtml이라는*새 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="a2642-180">기존 콘텐츠를 다음 내용으로 바꿉꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="a2642-181">코드는 먼저 `DataSet` 개체를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="a2642-182">이 개체는 XML 파일에서 읽은 데이터를 관리하고 스키마 파일의 정보에 따라 구성하는 데 사용됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="a2642-183">(코드의 맨 위에는 명령문이 `using SystemData`포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="a2642-184">개체를 사용하여 `DataSet` 작업하려면 이 작업이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="a2642-185">자세한 내용은 이 문서의 후반부에서 [ &quot;명령문 및&quot; 정규화된 이름 사용을](#SB_UsingStatements) 참조하십시오.</span><span class="sxs-lookup"><span data-stu-id="a2642-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="a2642-186">다음으로 코드는 데이터 `DataView` 집합을 기반으로 개체를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="a2642-187">데이터 뷰는 차트가 &#8212;, 읽기 및 플롯에 바인딩할 수 있는 개체를 제공합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="a2642-188">이 `AddSeries` 시간 `xValue` 및 `yValues` 매개 변수가 `DataView` 개체로 설정된 경우를 제외하고, 배열 데이터를 차트화할 때 앞서 보았듯이 차트는 메서드를 사용하여 데이터에 바인딩됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="a2642-189">이 예제에서는 특정 차트 유형을 지정하는 방법도 보여 주어 도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="a2642-190">메서드에 데이터가 추가되면 `AddSeries` `chartType` 매개 변수도 원형 차트를 표시하도록 설정됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="a2642-191">브라우저에서 페이지를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="a2642-192">"사용" 명령문 및 정규화된 이름</span><span class="sxs-lookup"><span data-stu-id="a2642-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="a2642-193">Razor 구문을 ASP.NET 웹 페이지를 ASP.NET .NET 프레임워크는 수천 개의 구성 요소(클래스)로 구성됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="a2642-194">이러한 모든 클래스에서 작업할 수 있도록 라이브러리와 *namespaces*다소 비슷합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="a2642-195">예를 들어 `System.Web` 네임스페이스에는 브라우저/서버 통신을 `System.Xml` 지원하는 클래스가 포함되고, 네임스페이스에는 XML `System.Data` 파일을 만들고 읽는 데 사용되는 클래스가 포함되며, 네임스페이스에는 데이터로 작업할 수 있는 클래스가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="a2642-196">.NET Framework에서 지정된 클래스에 액세스하려면 코드는 클래스 이름뿐만 아니라 클래스가 있는 네임스페이스도 알아야 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="a2642-197">예를 들어 `Chart` 도우미를 사용하려면 코드는 `System.Web.Helpers.Chart` 네임스페이스()와`System.Web.Helpers`클래스 이름()을`Chart`결합한 클래스를 찾아야 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="a2642-198">이 이름은 .NET Framework의 광대 함 내에서 완전하고 명확한 위치에 &#8212; 클래스의 *정식 자격으로* 된 이름으로 알려져 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="a2642-199">코드에서 다음과 같이 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="a2642-200">그러나 클래스 나 도우미를 참조 할 때마다 이러한 길고 정규화된 이름을 사용해야하는 것은 번거롭고 오류가 발생하기 쉽습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="a2642-201">따라서 클래스 이름을 더 쉽게 사용할 수 있도록 .NET Framework의 여러 네임스페이스 중에서 소수에 불과한 관심 있는 네임스페이스를 *가져올* 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="a2642-202">네임스페이스를 가져온 경우 정규화된 이름()`Chart``System.Web.Helpers.Chart`대신 클래스 이름()만 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="a2642-203">코드가 실행되고 클래스 이름이 발생하면 가져온 네임스페이스만 사용하여 해당 클래스를 찾을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="a2642-204">Razor 구문이 있는 ASP.NET 웹 페이지를 사용하여 웹 페이지를 만드는 경우 일반적으로 `WebPage` 클래스, 다양한 도우미 등을 포함하여 매번 동일한 클래스 집합을 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="a2642-205">웹 사이트를 만들 때마다 관련 네임스페이스를 가져오는 작업을 저장하기 위해 모든 웹 사이트에 대해 핵심 네임스페이스 집합을 자동으로 가져오도록 ASP.NET 구성됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="a2642-206">그렇기 때문에 네임스페이스를 다루거나 지금까지 가져오지 않아도 됩니다. 작업한 모든 클래스는 이미 가져온 네임스페이스에 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="a2642-207">그러나 경우에 따라 자동으로 가져오는 네임스페이스에 없는 클래스로 작업해야 하는 경우가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="a2642-208">이 경우 해당 클래스의 정규화된 이름을 사용하거나 클래스가 포함된 네임스페이스를 수동으로 가져올 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="a2642-209">네임스페이스를 가져오려면 문서 `using` 의`import` 이전 예제에서 보았듯이 명령문(Visual Basic)을 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="a2642-210">예를 들어 `DataSet` 클래스는 네임스페이스에 `System.Data` 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="a2642-211">Razor `System.Data` 페이지에서네임스페이스를 ASP.NET 자동으로 사용할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="a2642-212">따라서 정규화된 이름을 `DataSet` 사용하여 클래스와 함께 작업하려면 다음과 같은 코드를 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="a2642-213">클래스를 `DataSet` 반복적으로 사용해야하는 경우 다음과 같은 네임 스페이스를 가져 온 다음 코드에서 클래스 이름만 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="a2642-214">참조하려는 `using` 다른 .NET Framework 네임스페이스에 대한 문을 추가할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="a2642-215">그러나 언급했듯이 작업할 대부분의 클래스는 *.cshtml* 및 *.vbhtml* 페이지에서 사용하기 위해 ASP.NET 자동으로 가져오는 네임스페이스에 있기 때문에 이 작업을 자주 수행할 필요가 없습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="a2642-216">웹 페이지 내부에 차트 표시</span><span class="sxs-lookup"><span data-stu-id="a2642-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="a2642-217">지금까지 본 예제에서는 차트를 만든 다음 차트를 그래픽으로 브라우저에 직접 렌더링합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="a2642-218">그러나 대부분의 경우 브라우저에서차트뿐만 아니라 페이지의 일부로 차트를 표시하려고 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="a2642-219">이렇게 하려면 2단계 프로세스가 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-219">To do that requires a two-step process.</span></span> <span data-ttu-id="a2642-220">첫 번째 단계는 이미 본 것처럼 차트를 생성하는 페이지를 만드는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="a2642-221">두 번째 단계는 결과 이미지를 다른 페이지에 표시하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="a2642-222">이미지를 표시하려면 이미지를 표시하는 `<img>` 것과 동일한 방식으로 HTML 요소를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="a2642-223">그러나 *.jpg* 또는 *.png* 파일을 참조하는 대신 `<img>` 차트를 만드는 `Chart` 도우미가 포함된 *.cshtml* 파일을 참조합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="a2642-224">표시 페이지가 실행되면 `<img>` 요소는 도우미의 `Chart` 출력을 얻고 차트를 렌더링합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="a2642-225">*ShowChart.cshtml이라는*파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="a2642-226">기존 콘텐츠를 다음 내용으로 바꿉꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="a2642-227">코드는 `<img>` 요소를 사용하여 *ChartArrayBasic.cshtml* 파일에서 이전에 만든 차트를 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="a2642-228">브라우저에서 웹 페이지를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-228">Run the web page in a browser.</span></span> <span data-ttu-id="a2642-229">*ShowChart.cshtml* 파일은 *ChartArrayBasic.cshtml* 파일에 포함된 코드를 기반으로 차트 이미지를 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="a2642-230">차트 스타일 지정</span><span class="sxs-lookup"><span data-stu-id="a2642-230">Styling a Chart</span></span>

<span data-ttu-id="a2642-231">도우미는 `Chart` 차트의 모양을 사용자 지정할 수 있는 많은 옵션을 지원합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="a2642-232">색상, 글꼴, 테두리 등을 설정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="a2642-233">차트의 모양을 사용자 지정하는 쉬운 방법은 *테마를*사용하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="a2642-234">테마는 글꼴, 색, 레이블, 색상표, 테두리 및 효과를 사용하여 렌더링하는 방법을 지정하는 정보의 모음입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="a2642-235">차트 의 스타일은 차트 유형을 나타내지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="a2642-236">다음 표에는 기본 제공 테마가 나열되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="a2642-237">테마</span><span class="sxs-lookup"><span data-stu-id="a2642-237">Theme</span></span> | <span data-ttu-id="a2642-238">설명</span><span class="sxs-lookup"><span data-stu-id="a2642-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="a2642-239">흰색 배경에 빨간색 열을 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="a2642-240">파란색 그라데이션 배경에 파란색 열을 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="a2642-241">녹색 그라데이션 배경에 파란색 열을 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="a2642-242">노란색 그라데이션 배경에 주황색 열을 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="a2642-243">흰색 배경에 3D 빨간색 열을 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="a2642-244">새 차트를 만들 때 사용할 테마를 지정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="a2642-245">*ChartStyleGreen.cshtml이라는*새 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="a2642-246">페이지의 기존 콘텐츠를 다음으로 바꿉습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="a2642-247">이 코드는 데이터에 데이터베이스를 사용 하지만 개체를 `theme` 만들 때 매개 변수를 `Chart` 추가 하는 이전 예제와 동일 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="a2642-248">다음은 변경된 코드를 보여 주며,</span><span class="sxs-lookup"><span data-stu-id="a2642-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="a2642-249">브라우저에서 페이지를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-249">Run the page in a browser.</span></span> <span data-ttu-id="a2642-250">이전과 동일한 데이터가 표시되지만 차트는 더 세련된 것처럼 보입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="a2642-251">차트 저장</span><span class="sxs-lookup"><span data-stu-id="a2642-251">Saving a Chart</span></span>

<span data-ttu-id="a2642-252">이 문서에서 `Chart` 지금까지 보았듯이 도우미를 사용하면 도우미가 호출될 때마다 처음부터 차트를 다시 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="a2642-253">필요한 경우 차트의 코드는 데이터베이스를 다시 쿼리하거나 XML 파일을 다시 읽고 데이터를 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="a2642-254">경우에 따라 쿼리하는 데이터베이스가 큰 경우 또는 XML 파일에 많은 데이터가 포함된 경우와 같이 복잡한 작업이 될 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="a2642-255">차트에 많은 데이터가 포함되지 않더라도 이미지를 동적으로 만드는 프로세스는 서버 리소스를 차지하며 많은 사람들이 차트를 표시하는 페이지 나 페이지를 요청하는 경우 웹 사이트의 성능에 영향을 미칠 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="a2642-256">차트를 만들 때 발생할 수 있는 성능 영향을 줄이려면 차트를 처음 작성한 다음 저장할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="a2642-257">차트를 다시 생성하는 대신 다시 필요한 경우 저장된 버전을 가져와렌더링할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="a2642-258">다음과 같은 방법으로 차트를 저장할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="a2642-259">(서버)에서 컴퓨터 메모리에 차트를 캐시합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="a2642-260">차트를 이미지 파일로 저장합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="a2642-261">차트를 XML 파일로 저장합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-261">Save the chart as an XML file.</span></span> <span data-ttu-id="a2642-262">이 옵션을 사용하면 차트를 저장하기 전에 차트를 수정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="a2642-263">차트 캐싱</span><span class="sxs-lookup"><span data-stu-id="a2642-263">Caching a Chart</span></span>

<span data-ttu-id="a2642-264">차트를 만든 후 차트를 캐시할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="a2642-265">차트를 캐싱하면 차트를 다시 표시해야 하는 경우 차트를 다시 만들 필요가 없습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="a2642-266">차트를 캐시에 저장하면 해당 차트에 고유해야 하는 키를 지정합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="a2642-267">서버의 메모리부족이 면 캐시에 저장된 차트가 제거될 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="a2642-268">또한 어떤 이유로든 응용 프로그램이 다시 시작되면 캐시가 지워집니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="a2642-269">따라서 캐시된 차트로 작업하는 표준 방법은 항상 캐시에서 사용할 수 있는지 여부를 먼저 확인한 다음 만들지 않으면 만들거나 다시 만드는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="a2642-270">웹 사이트의 루트에서 *ShowCachedChart.cshtml이라는*파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="a2642-271">기존 콘텐츠를 다음 내용으로 바꿉꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="a2642-272">태그에는 `<img>` `src` *ChartSaveToCache.cshtml* 파일을 가리키고 쿼리 문자열로 페이지에 키를 전달하는 특성이 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="a2642-273">키에는 myChartKey &quot;&quot;값이 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="a2642-274">*ChartSaveToCache.cshtml* 파일에는 `Chart` 차트를 만드는 도우미가 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="a2642-275">잠시 후 이 페이지를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="a2642-276">페이지 *끝에ClearCache.cshtml*이라는 페이지에 대 한 링크가 있다.</span><span class="sxs-lookup"><span data-stu-id="a2642-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="a2642-277">곧 만들 페이지입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="a2642-278">이 예제에 대한 캐싱을 테스트하려면 *ClearCache.cshtml이* 필요합니다 .</span><span class="sxs-lookup"><span data-stu-id="a2642-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="a2642-279">웹 사이트의 루트에서 *ChartSaveToCache.cshtml이라는*새 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="a2642-280">기존 콘텐츠를 다음 내용으로 바꿉꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="a2642-281">코드는 먼저 쿼리 문자열의 키 값으로 전달된 것이 있는지 여부를 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="a2642-282">이 경우 코드는 `GetFromCache` 메서드를 호출하고 키를 전달하여 캐시에서 차트를 읽으려고 시도합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="a2642-283">해당 키 아래의 캐시에 차트가 처음 요청될 때 발생하는 것이 없는 것으로 판명되면 코드는 평소와 같이 차트를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="a2642-284">차트가 완료되면 코드는 을 호출하여 `SaveToCache`캐시에 저장합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="a2642-285">이 메서드에는 키가 필요하므로 나중에 차트를 요청할 수 있으며 차트를 캐시에 저장해야 하는 시간이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="a2642-286">차트를 캐시하는 정확한 시간은 차트가 나타내는 데이터가 변경될 수 있다고 생각한 빈도에 따라 달라집니다. 또한 `SaveToCache` 이 메서드는 차트에 `slidingExpiration` 액세스할 때마다 시간 시간 지정 카운터가 재설정되는 경우 매개 변수 &#8212; 이 방법을 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="a2642-287">이 경우 실제로 는 차트의 캐시 항목이 마지막으로 차트에 액세스한 후 2분 후에 만료됨을 의미합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="a2642-288">(슬라이딩 만료에 대한 대안은 절대 만료이므로 캐시 항목이 액세스 빈도에 관계없이 캐시에 넣은 후 정확히 2분 후에 만료됩니다.)</span><span class="sxs-lookup"><span data-stu-id="a2642-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="a2642-289">마지막으로 코드는 메서드를 `WriteFromCache` 사용하여 캐시에서 차트를 가져오고 렌더링합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="a2642-290">이 메서드는 차트가 `if` 시작되었거나 캐시에 저장해야 하는지 여부에 관계없이 캐시에서 차트를 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="a2642-291">예제에서 메서드에는 `AddTitle` 타임스탬프가 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="a2642-292">(제목에 &#8212; &#8212; `DateTime.Now` 현재 날짜와 시간을 추가합니다.)</span><span class="sxs-lookup"><span data-stu-id="a2642-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="a2642-293">*ClearCache.cshtml이라는* 새 페이지를 만들고 해당 콘텐츠를 다음 으로 바꿉니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="a2642-294">이 페이지는 `WebCache` 도우미를 사용하여 *ChartSaveToCache.cshtml에*캐시된 차트를 제거합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="a2642-295">앞서 언급했듯이 일반적으로 이와 같은 페이지가 있을 필요는 없습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="a2642-296">캐싱을 더 쉽게 테스트할 수 있도록 여기에만 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="a2642-297">브라우저에서 *ShowCachedChart.cshtml* 웹 페이지를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="a2642-298">이 페이지에는 *ChartSaveToCache.cshtml* 파일에 포함된 코드를 기반으로 차트 이미지가 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="a2642-299">차트 제목에 타임스탬프가 말하는 내용을 기록해 둡니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![설명: 차트 제목에 타임스탬프가 있는 기본 차트 그림](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="a2642-301">브라우저를 닫습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-301">Close the browser.</span></span>
8. <span data-ttu-id="a2642-302">*쇼캐시차트.cshtml을* 다시 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="a2642-303">타임스탬프는 차트가 재생성되지 않고 대신 캐시에서 읽었음을 나타내는 이전과 동일합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="a2642-304">*ShowCachedChart.cshtml에서*캐시 **지우기** 링크를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="a2642-305">이렇게 하면 캐시가 지워졌다고 보고하는 *ClearCache.cshtml로*이동합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="a2642-306">**쇼캐시차트로 돌아가기** 링크를 클릭하거나 웹매트릭스에서 *ShowCachedChart.cshtml을* 다시 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="a2642-307">캐시가 지워졌기 때문에 이번에는 타임스탬프가 변경되었습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="a2642-308">따라서 코드는 차트를 다시 생성하고 캐시에 다시 넣어야 했습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="a2642-309">차트를 이미지 파일로 저장</span><span class="sxs-lookup"><span data-stu-id="a2642-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="a2642-310">차트를 서버에 이미지 파일(예: *.jpg* 파일)으로 저장할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="a2642-311">그런 다음 이미지 파일을 이미지와 같은 방식으로 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="a2642-312">장점은 파일이 임시 캐시에 저장되지 않고 저장된다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="a2642-313">새 차트 이미지를 서로 다른 시간(예: 매시간)에 저장한 다음 시간에 따라 발생하는 변경 내용을 영구적으로 기록할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="a2642-314">웹 응용 프로그램에 이미지 파일을 넣을 서버의 폴더에 파일을 저장할 수 있는 권한이 있는지 확인해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="a2642-315">웹 사이트의 루트에서 아직 존재하지 않는 경우 \* \_ChartFiles라는\* 폴더를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="a2642-316">웹 사이트의 루트에서 *ChartSave.cshtml이라는*새 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="a2642-317">기존 콘텐츠를 다음 내용으로 바꿉꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="a2642-318">코드는 먼저 `File.Exists` 메서드를 호출하여 *.jpg* 파일이 있는지 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="a2642-319">파일이 없으면 코드는 배열에서 새 `Chart` 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="a2642-320">이번에는 코드가 메서드를 `Save` 호출하고 매개 `path` 변수를 전달하여 차트를 저장할 위치의 파일 경로 및 파일 이름을 지정합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="a2642-321">페이지 본문에서 `<img>` 요소는 *표시할 .jpg* 파일을 가리키는 경로를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="a2642-322">*ChartSave.cshtml 파일을 실행합니다.*</span><span class="sxs-lookup"><span data-stu-id="a2642-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="a2642-323">웹매트릭스로 돌아갑니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-323">Return to WebMatrix.</span></span> <span data-ttu-id="a2642-324">*chart01.jpg라는* 이미지 파일이 \* \_ChartFiles\* 폴더에 저장되었습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="a2642-325">차트를 XML 파일로 저장</span><span class="sxs-lookup"><span data-stu-id="a2642-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="a2642-326">마지막으로 차트를 서버에 XML 파일로 저장할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="a2642-327">차트를 캐싱하거나 차트를 파일에 저장하는 데 이 방법을 사용하면 원하는 경우 차트를 표시하기 전에 XML을 수정할 수 있다는 이점이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="a2642-328">응용 프로그램에는 이미지 파일을 넣을 서버의 폴더에 대한 읽기/쓰기 권한이 있어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="a2642-329">웹 사이트의 루트에서 *ChartSaveXml.cshtml이라는*새 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="a2642-330">기존 콘텐츠를 다음 내용으로 바꿉꿉입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="a2642-331">이 코드는 XML 파일을 사용한다는 점을 제외하면 캐시에 차트를 저장하기 위해 이전에 본 코드와 유사합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="a2642-332">코드는 먼저 `File.Exists` 메서드를 호출하여 XML 파일이 있는지 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="a2642-333">파일이 있는 경우 코드는 새 `Chart` 개체를 만들고 파일 `themePath` 이름을 매개 변수로 전달합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="a2642-334">이렇게 하면 XML 파일의 모든 것을 기반으로 차트가 만들어집니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="a2642-335">XML 파일이 아직 존재하지 않는 경우 코드는 정상과 같은 `SaveXml` 차트를 만들고 이를 저장하기 위해 호출합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="a2642-336">차트는 이전에 보았듯이 `Write` 메서드를 사용하여 렌더링됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="a2642-337">캐싱을 보여 준 페이지와 마찬가지로 이 코드에는 차트 제목에 타임스탬프가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="a2642-338">*ChartDisplayXMLChart.cshtml이라는* 새 페이지를 만들고 다음 마크업을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="a2642-339">*차트디스플레이XML차트.cshtml* 페이지를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="a2642-340">차트가 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-340">The chart is displayed.</span></span> <span data-ttu-id="a2642-341">차트 제목에 있는 타임스탬프를 기록해 둡니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="a2642-342">브라우저를 닫습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-342">Close the browser.</span></span>
6. <span data-ttu-id="a2642-343">WebMatrix에서 \* \_ChartFiles\* 폴더를 마우스 오른쪽 단추로 클릭하고 **새로 고침을**클릭한 다음 폴더를 엽니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="a2642-344">이 폴더의 *XMLChart.xml* 파일은 `Chart` 도우미에 의해 만들어졌습니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![설명: 차트 도우미에 의해 생성된 XMLChart.xml 파일을 보여주는 _ChartFiles 폴더입니다.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="a2642-346">*ChartDisplayXMLChart.cshtml* 페이지를 다시 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="a2642-347">차트에는 페이지를 처음 실행했을 때와 동일한 타임스탬프가 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="a2642-348">이전에 저장한 XML에서 차트가 생성되기 때문입니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="a2642-349">웹매트릭스에서 \* \_ChartFiles\* 폴더를 열고 *XMLChart.xml* 파일을 삭제합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="a2642-350">*ChartDisplayXMLChart.cshtml* 페이지를 한 번 더 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="a2642-351">이번에는 `Chart` 도우미가 XML 파일을 다시 만들어야 했기 때문에 타임스탬프가 업데이트됩니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="a2642-352">원하는 경우 \* \_ChartFiles\* 폴더를 확인하고 XML 파일이 돌아왔다는 것을 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="a2642-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="a2642-353">추가 리소스</span><span class="sxs-lookup"><span data-stu-id="a2642-353">Additional Resources</span></span>

- [<span data-ttu-id="a2642-354">ASP.NET 웹 페이지 사이트의 데이터베이스 작업 소개</span><span class="sxs-lookup"><span data-stu-id="a2642-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="a2642-355">ASP.NET 웹 페이지 사이트에서 캐싱을 사용하여 성능 향상</span><span class="sxs-lookup"><span data-stu-id="a2642-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="a2642-356">[차트](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) 클래스(MSDN의 웹 페이지 API 참조 ASP.NET)</span><span class="sxs-lookup"><span data-stu-id="a2642-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
