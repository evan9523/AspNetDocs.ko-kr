---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: ASP.NET 웹 페이지 소개 - HTML 양식 기본 사항 | 마이크로 소프트 문서
author: Rick-Anderson
description: 이 자습서에서는 입력 양식을 만드는 방법과 ASP.NET 웹 페이지(Razor)를 사용할 때 사용자의 입력을 처리하는 방법에 대한 기본 정보를 보여 주며, 이 자습서에서는 입력 양식을 만드는 방법에 대해 설명합니다. 그리고 지금 당신은 ...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 04/06/2020
ms.locfileid: "80675924"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="d2ad9-104">ASP.NET 웹 페이지 소개 - HTML 양식 기본 사항</span><span class="sxs-lookup"><span data-stu-id="d2ad9-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="d2ad9-105">[Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="d2ad9-106">이 자습서에서는 입력 양식을 만드는 방법과 ASP.NET 웹 페이지(Razor)를 사용할 때 사용자의 입력을 처리하는 방법에 대한 기본 정보를 보여 주며, 이 자습서에서는 입력 양식을 만드는 방법에 대해 설명합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="d2ad9-107">이제 데이터베이스가 있으므로 양식 기술을 사용하여 사용자가 데이터베이스에서 특정 동영상을 찾을 수 있도록 합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="d2ad9-108">웹 페이지를 사용하여 데이터 표시 소개를 통해 시리즈를 완료했다고 [가정 ASP.NET합니다.](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="d2ad9-109">학습할 내용:</span><span class="sxs-lookup"><span data-stu-id="d2ad9-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="d2ad9-110">표준 HTML 요소를 사용하여 양식을 만드는 방법.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="d2ad9-111">양식에서 사용자의 입력을 읽는 방법.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="d2ad9-112">사용자가 제공하는 검색어를 사용하여 데이터를 선택적으로 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="d2ad9-113">페이지에 사용자가 입력한 내용을 "기억"하는 필드를 갖도록 하는 방법</span><span class="sxs-lookup"><span data-stu-id="d2ad9-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="d2ad9-114">논의된 기능/기술:</span><span class="sxs-lookup"><span data-stu-id="d2ad9-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="d2ad9-115">`Request` 개체입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-115">The `Request` object.</span></span>
> - <span data-ttu-id="d2ad9-116">SQL `Where` 절입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="d2ad9-117">만들 내용</span><span class="sxs-lookup"><span data-stu-id="d2ad9-117">What You'll Build</span></span>

<span data-ttu-id="d2ad9-118">이전 자습서에서는 데이터베이스를 만들고, 데이터를 추가한 다음, 도우미를 `WebGrid` 사용하여 데이터를 표시했습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="d2ad9-119">이 자습서에서는 특정 장르의 영화를 찾을 수 있는 검색 상자를 추가하거나 제목에 입력한 단어가 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="d2ad9-120">(예를 들어 장르가 "액션"이거나 제목에 "해리" 또는 "어드벤처"가 포함된 모든 영화를 찾을 수 있습니다.)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="d2ad9-121">이 자습서를 완료하면 다음과 같은 페이지가 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![장르 및 제목 검색이 있는 동영상 페이지](form-basics/_static/image1.png)

<span data-ttu-id="d2ad9-123">페이지의 목록 부분은 마지막 자습서 &mdash; 그리드와 동일합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="d2ad9-124">차이점은 그리드에 검색한 영화만 표시된다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="d2ad9-125">HTML 양식 정보</span><span class="sxs-lookup"><span data-stu-id="d2ad9-125">About HTML Forms</span></span>

<span data-ttu-id="d2ad9-126">HTML 양식을 만든 경험이 있고 `GET` 이 `POST`섹션과 의 차이점이 있는 경우 이 섹션을 건너뛸 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="d2ad9-127">양식에는 사용자 입력 &mdash; 요소 텍스트 상자, 단추, 라디오 단추, 확인란, 드롭다운 목록 등이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="d2ad9-128">사용자는 이러한 컨트롤을 입력하거나 선택한 다음 단추를 클릭하여 양식을 제출합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="d2ad9-129">양식의 기본 HTML 구문은 이 예제에서 설명합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="d2ad9-130">이 태그가 페이지에서 실행되면 다음과 같은 간단한 양식을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![브라우저에서 렌더링된 기본 HTML 양식](form-basics/_static/image2.png)

<span data-ttu-id="d2ad9-132">요소는 `<form>` 제출할 HTML 요소를 둘러싸는다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="d2ad9-133">(쉬운 실수는 페이지에 요소를 추가한 다음 `<form>` 요소 안에 넣는 것을 잊어 버리는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="d2ad9-134">이 경우 아무 것도 제출되지 않습니다.) 속성은 `method` 브라우저에 사용자 입력을 제출하는 방법을 알려줍니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="d2ad9-135">서버에서 업데이트를 `post` 수행하는 경우 또는 서버에서 데이터를 `get` 가져오는 경우로 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="d2ad9-136">**GET, POST 및 HTTP 동사 안전**</span><span class="sxs-lookup"><span data-stu-id="d2ad9-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="d2ad9-137">브라우저와 서버가 정보를 교환하는 데 사용하는 프로토콜인 HTTP는 기본 작업에서 매우 간단합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="d2ad9-138">브라우저는 몇 개의 동사만 사용하여 서버에 요청을 합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="d2ad9-139">웹에 대한 코드를 작성할 때 이러한 동사와 브라우저및 서버에서 코드를 사용하는 방법을 이해하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="d2ad9-140">가장 일반적으로 사용되는 동사는 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="d2ad9-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-141">`GET`.</span></span> <span data-ttu-id="d2ad9-142">브라우저는 이 동사를 사용하여 서버에서 무언가를 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="d2ad9-143">예를 들어 브라우저에 URL을 입력하면 브라우저에서 `GET` 원하는 페이지를 요청하는 작업을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="d2ad9-144">페이지에 그래픽이 포함된 경우 브라우저는 이미지를 얻기 위해 추가 `GET` 작업을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="d2ad9-145">`GET` 작업이 서버에 정보를 전달해야 하는 경우 정보는 쿼리 문자열의 URL의 일부로 전달됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="d2ad9-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-146">`POST`.</span></span> <span data-ttu-id="d2ad9-147">브라우저는 서버에 `POST` 추가하거나 변경할 데이터를 제출하기 위해 요청을 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="d2ad9-148">예를 들어 `POST` 동사는 데이터베이스에서 레코드를 만들거나 기존 레코드를 변경하는 데 사용됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="d2ad9-149">대부분의 경우 양식을 작성하고 제출 단추를 클릭하면 브라우저에서 작업을 `POST` 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="d2ad9-150">`POST` 작업에서 서버로 전달되는 데이터는 페이지 본문에 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="d2ad9-151">이러한 동사 간의 중요한 차이점은 `GET` 작업이 서버에서 아무 것도 변경하지 않거나 약간 추상적인 방식으로 `GET` 작업하면 서버의 상태가 변경되지 않는다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="d2ad9-152">동일한 리소스에서 `GET` 작업을 원하는 횟수만큼 수행할 수 있으며 이러한 리소스는 변경되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="d2ad9-153">`GET` (수술은 종종 "안전"이라고하거나 기술 용어를 사용하는 것은 *idempotent입니다.)* 반대로 작업을 수행할 때마다 `POST` 요청이 서버에서 변경됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="d2ad9-154">두 가지 예는 이러한 구분을 설명하는 데 도움이 됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="d2ad9-155">Bing 또는 Google과 같은 엔진을 사용하여 검색을 수행하면 하나의 텍스트 상자로 구성된 양식을 입력한 다음 검색 단추를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="d2ad9-156">브라우저는 입력한 `GET` 상자에 입력한 값이 URL의 일부로 전달되는 작업을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="d2ad9-157">검색 `GET` 작업은 서버의 리소스를 변경하지 않으므로 이러한 형식의 양식에 대한 작업을 사용하는 것은 괜찮습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="d2ad9-158">이제 온라인으로 무언가를 주문하는 과정을 고려하십시오.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="d2ad9-159">주문 세부 정보를 입력한 다음 제출 단추를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="d2ad9-160">이 작업은 새 `POST` 주문 레코드, 계정 정보 변경 및 기타 여러 변경 사항과 같은 서버에서 변경되므로 요청이 됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="d2ad9-161">작업과 `GET` 달리 `POST` 요청을 반복할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="d2ad9-162">(이와 같은 경우 웹 사이트는 제출 단추를 두 번 이상 클릭하지 말라고 경고하거나 실수로 양식을 다시 제출하지 않도록 제출 단추를 비활성화합니다.)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="d2ad9-163">이 자습서에서는 HTML 양식으로 `GET` 작업하는 작업과 `POST` 작업을 모두 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="d2ad9-164">각 경우에 사용하는 동사가 적절한 동사인 이유를 설명합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="d2ad9-165">HTTP 동사에 대한 자세한 내용은 W3C 사이트의 [메서드 정의](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) 문서를 참조하십시오.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="d2ad9-166">대부분의 사용자 입력 `<input>` 요소는 HTML 요소입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="d2ad9-167">*형식은* `<input type="type" name="name">,` 원하는 사용자 입력 컨트롤의 종류를 나타내는 위치처럼 보입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="d2ad9-168">이러한 요소는 다음과 같은 일반적인 요소입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-168">These elements are the common ones:</span></span>

- <span data-ttu-id="d2ad9-169">텍스트 상자:`<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="d2ad9-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="d2ad9-170">확인란:`<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="d2ad9-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="d2ad9-171">라디오 버튼:`<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="d2ad9-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="d2ad9-172">단추:`<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="d2ad9-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="d2ad9-173">제출 버튼:`<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="d2ad9-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="d2ad9-174">`<textarea>` 요소를 사용하여 다중 줄 텍스트 상자와 `<select>` 요소를 만들어 드롭다운 목록 또는 스크롤 가능한 목록을 만들 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="d2ad9-175">HTML 양식 요소에 대한 자세한 내용은 W3Schools 사이트의 [HTML 양식 및 입력을](http://www.w3schools.com/html/html_forms.asp) 참조하십시오.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="d2ad9-176">`name` 이름은 나중에 요소의 값을 얻는 방법이므로 곧 볼 수 있으므로 특성이 매우 중요합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="d2ad9-177">흥미로운 부분은 페이지 개발자인 사용자가 사용자의 입력을 통해 수행하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="d2ad9-178">이러한 요소와 관련된 기본 제공 동작은 없습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="d2ad9-179">대신 사용자가 입력하거나 선택한 값을 얻고 그들과 함께 작업을 수행해야합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="d2ad9-180">이것이 이 자습서에서 배울 수 있는 내용입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="d2ad9-181">**HTML5 및 입력 양식**</span><span class="sxs-lookup"><span data-stu-id="d2ad9-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="d2ad9-182">아시다시피 HTML은 전환 중이며 최신 버전(HTML5)에는 사용자가 정보를 입력할 수 있는 보다 직관적인 방법에 대한 지원이 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="d2ad9-183">예를 들어 HTML5에서 페이지 개발자는 사용자가 날짜를 입력하도록 페이지를 알릴 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="d2ad9-184">그러면 브라우저는 사용자가 수동으로 날짜를 입력하도록 요구하지 않고 캘린더를 자동으로 표시할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="d2ad9-185">그러나 HTML5는 새 것이며 아직 모든 브라우저에서 지원되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="d2ad9-186">ASP.NET 웹 페이지는 사용자의 브라우저가 수행하는 범위까지 HTML5 입력을 지원합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="d2ad9-187">HTML5의 `<input>` 요소에 대한 새 특성에 대한 아이디어는 W3Schools 사이트의 [HTML &lt;&gt; 입력 유형 특성을](http://www.w3schools.com/html/html_form_input_types.asp) 참조하십시오.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="d2ad9-188">폼 만들기</span><span class="sxs-lookup"><span data-stu-id="d2ad9-188">Creating the Form</span></span>

<span data-ttu-id="d2ad9-189">WebMatrix에서 **파일** 작업 영역에서 *Movies.cshtml* 페이지를 엽니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="d2ad9-190">닫는 `</h1>` 태그 후 호출의 `<div>` `grid.GetHtml` 열기 태그 앞에 다음 태그를 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="d2ad9-191">이 태그는 명명된 `searchGenre` 텍스트 상자와 제출 단추가 있는 양식을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="d2ad9-192">텍스트 상자와 제출 단추는 `<form>` `method` 특성이 `get`로 설정된 요소에 동봉됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="d2ad9-193">(텍스트 상자를 넣고 요소 안에 단추를 `<form>` 제출하지 않으면 단추를 클릭하면 아무 것도 제출되지 않습니다.) 서버에서 `GET` 변경하지 않는 양식을 만들기 때문에 여기에서 동사를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="d2ad9-194">(이전 자습서에서는 서버에 변경 `post` 내용을 제출 하는 메서드를 사용 했습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="d2ad9-195">다음 자습서에서 다시 볼 수 있습니다.)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="d2ad9-196">페이지를 실행합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-196">Run the page.</span></span> <span data-ttu-id="d2ad9-197">양식에 대한 동작을 정의하지는 않았지만 다음과 같은 모양을 볼 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![장르에 대한 검색 상자가있는 영화 페이지](form-basics/_static/image3.png)

<span data-ttu-id="d2ad9-199">텍스트 상자에 '코미디'와 같은 값을 입력합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="d2ad9-200">그런 다음 **검색 장르를 클릭합니다.**</span><span class="sxs-lookup"><span data-stu-id="d2ad9-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="d2ad9-201">페이지의 URL을 기록해 둡것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-201">Take note of the URL of the page.</span></span> <span data-ttu-id="d2ad9-202">요소의 `<form>` `method` 특성을 `get`에 설정하기 때문에 입력한 값은 이제 다음과 같이 URL의 쿼리 문자열의 일부입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="d2ad9-203">양식 값 읽기</span><span class="sxs-lookup"><span data-stu-id="d2ad9-203">Reading Form Values</span></span>

<span data-ttu-id="d2ad9-204">페이지에는 데이터베이스 데이터를 얻고 결과를 그리드에 표시하는 일부 코드가 이미 포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="d2ad9-205">이제 검색어가 포함된 SQL 쿼리를 실행할 수 있도록 텍스트 상자의 값을 읽는 코드를 추가해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="d2ad9-206">양식의 메서드를 `get`으로 설정하면 다음과 같은 코드를 사용하여 텍스트 상자에 입력된 값을 읽을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="d2ad9-207">개체(개체의 `QueryString` 속성)에는 `GET` 작업의 일부로 제출된 요소의 값이 포함됩니다. `Request.QueryString` `Request`</span><span class="sxs-lookup"><span data-stu-id="d2ad9-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="d2ad9-208">속성에는 `Request.QueryString` 양식에 제출된 값의 컬렉션(목록)이 포함되어 있습니다. *collection*</span><span class="sxs-lookup"><span data-stu-id="d2ad9-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="d2ad9-209">개별 값을 얻으려면 원하는 요소의 이름을 지정합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="d2ad9-210">그래서 텍스트 상자를 만드는 `name` `<input>` 요소 ()에`searchTerm`특성이 있어야합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="d2ad9-211">개체에 `Request` 대한 자세한 내용은 나중에 [사이드바를](#BKMK_TheRequestObject) 참조하십시오.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="d2ad9-212">텍스트 상자의 값을 읽을 수 있을 만큼 간단합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="d2ad9-213">그러나 사용자가 텍스트 상자에 아무 것도 입력하지 않았지만 어쨌든 **검색을** 클릭한 경우 검색할 항목이 없기 때문에 해당 클릭을 무시할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="d2ad9-214">다음 코드는 이러한 조건을 구현하는 방법을 보여 주는 예제입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="d2ad9-215">(이 코드를 아직 추가할 필요는 없습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="d2ad9-216">테스트는 다음과 같은 방식으로 분해됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="d2ad9-217">`Request.QueryString["searchGenre"]`의 값을 가져옵니다. `<input>` `searchGenre`</span><span class="sxs-lookup"><span data-stu-id="d2ad9-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="d2ad9-218">메서드를 `IsEmpty` 사용하여 비어 있는지 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="d2ad9-219">이 메서드는 무언가(예: 양식 요소)에 값이 포함되어 있는지 여부를 확인하는 표준 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="d2ad9-220">그러나 정말, 당신은 따라서, 비어 *있지 않은* 경우에만 관심 ...</span><span class="sxs-lookup"><span data-stu-id="d2ad9-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="d2ad9-221">테스트 `!` 앞에 연산자를 `IsEmpty` 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="d2ad9-222">(연산자는 `!` 논리적 NOT을 의미합니다).</span><span class="sxs-lookup"><span data-stu-id="d2ad9-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="d2ad9-223">일반 영어에서 전체 `if` 조건은 다음과 같습니다 : *양식의 searchGenre 요소가 비어 있지 않으면 ...*</span><span class="sxs-lookup"><span data-stu-id="d2ad9-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="d2ad9-224">이 블록은 검색어를 사용하는 쿼리를 만들기 위한 단계를 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="d2ad9-225">다음 섹션에서 구성을 완료합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="d2ad9-226">**요청 개체**</span><span class="sxs-lookup"><span data-stu-id="d2ad9-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="d2ad9-227">개체에는 `Request` 페이지를 요청하거나 제출할 때 브라우저가 응용 프로그램에 보내는 모든 정보가 들어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="d2ad9-228">이 개체에는 텍스트 상자 값또는 업로드할 파일과 같이 사용자가 제공하는 모든 정보가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="d2ad9-229">또한 쿠키, URL 쿼리 문자열의 값(있는 경우), 실행 중인 페이지의 파일 경로, 사용자가 사용하는 브라우저 유형, 브라우저에서 설정된 언어 목록 등모든 종류의 추가 정보가 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="d2ad9-230">개체는 `Request` 값의 컬렉션(목록)입니다. *collection*</span><span class="sxs-lookup"><span data-stu-id="d2ad9-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="d2ad9-231">이름을 지정하여 컬렉션에서 개별 값을 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="d2ad9-232">개체는 `Request` 실제로 여러 하위 집합을 노출합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="d2ad9-233">예를 들어:</span><span class="sxs-lookup"><span data-stu-id="d2ad9-233">For example:</span></span>
> 
> - <span data-ttu-id="d2ad9-234">`Request.Form`요청이 `POST` 요청인 경우 `<form>` 제출된 요소 내의 요소에서 값을 제공합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="d2ad9-235">`Request.QueryString`URL의 쿼리 문자열에 있는 값만 제공합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="d2ad9-236">(와 같은 `http://mysite/myapp/page?searchGenre=action&page=2`URL에서 `?searchGenre=action&page=2` URL의 섹션은 쿼리 문자열입니다.)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="d2ad9-237">`Request.Cookies`컬렉션은 브라우저가 전송한 쿠키에 대한 액세스를 제공합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="d2ad9-238">제출된 양식에 있는 값을 얻으려면 을 사용할 `Request["name"]`수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="d2ad9-239">또는 보다 구체적인 버전(요청의 `Request.Form["name"]` `POST` 경우) 또는 `Request.QueryString["name"]` (요청의 경우) `GET` 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="d2ad9-240">물론, *이름은* 얻을 항목의 이름입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="d2ad9-241">원하는 항목의 이름은 사용 하려는 컬렉션 내에서 고유 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="d2ad9-242">객체가 `Request` 와 `Request.Form` `Request.QueryString`같은 하위 집합을 제공하는 이유입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="d2ad9-243">페이지에 라는 `userName` 양식 요소가 포함되어 있고 `userName`라는 *쿠키도* 포함되어 있다고 가정합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="d2ad9-244">이 경우 `Request["userName"]`양식 값또는 쿠키를 사용할지 여부가 모호합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="d2ad9-245">그러나 얻을 `Request.Form["userName"]` 경우 `Request.Cookie["userName"]`또는 ' 얻을 값에 대 한 명시 적 되 고.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="d2ad9-246">구체적으로 설정하고 관심 `Request` `Request.Form` `Request.QueryString`있는 하위 집합을 사용하는 것이 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="d2ad9-247">이 자습서에서 만드는 간단한 페이지의 경우 실제로 아무런 차이가 없을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="d2ad9-248">그러나 더 복잡한 페이지를 만들 때 명시적 버전을 `Request.Form` 사용하거나 `Request.QueryString` 페이지에 양식(또는 여러 양식), 쿠키, 쿼리 문자열 값 등이 포함될 때 발생할 수 있는 문제를 방지할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="d2ad9-249">검색어를 사용하여 쿼리 만들기</span><span class="sxs-lookup"><span data-stu-id="d2ad9-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="d2ad9-250">이제 사용자가 입력한 검색어를 얻는 방법을 알고 있으므로 이를 사용하는 쿼리를 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="d2ad9-251">데이터베이스에서 모든 동영상 항목을 얻으려면 이 명령문과 같은 SQL 쿼리를 사용하고 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="d2ad9-252">특정 동영상만 얻으려면 절이 포함된 쿼리를 `Where` 사용해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="d2ad9-253">이 절을 사용하면 쿼리에서 반환되는 행조건을 설정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="d2ad9-254">예를 들면 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="d2ad9-255">기본 형식은 `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="d2ad9-256">원하는 항목에 따라 (보다 `=` `>` 큰), `<` (작음), `<>` (같지 않음), `<=` (작거나 같음) 등 외에 다른 연산을 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="d2ad9-257">궁금해하는 경우 SQL 문은 대/소문자를 &mdash; `SELECT` 구분하지 `Select` 않는 것이 `select`(또는 심지어)와 동일합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="d2ad9-258">그러나 사람들은 읽기 쉽도록 SQL `SELECT` 문과 `WHERE`의 키워드를 대문자로 대하는 경우가 많습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="d2ad9-259">검색어를 매개 변수로 전달</span><span class="sxs-lookup"><span data-stu-id="d2ad9-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="d2ad9-260">특정 장르를 검색하는 것은`WHERE Genre = 'Action'`충분히 쉽지만()는 사용자가 입력하는 모든 장르를 검색할 수 있기를 원합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="d2ad9-261">이렇게 하려면 검색할 값에 대 한 자리 표시자를 포함 하는 SQL 쿼리로 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="d2ad9-262">이 명령처럼 보입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="d2ad9-263">자리 표시자는 `@` 문자 다음에 0입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="d2ad9-264">추측할 수 있듯이 쿼리에는 여러 자리 표시자가 포함될 `@0`수 `@1` `@2`있으며 쿼리의 이름은 ', 등입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="d2ad9-265">쿼리를 설정하고 실제로 값을 전달하려면 다음과 같은 코드를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="d2ad9-266">이 코드는 그리드에 데이터를 표시하기 위해 이미 수행한 것과 유사합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="d2ad9-267">유일한 차이점은 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-267">The only differences are:</span></span>

- <span data-ttu-id="d2ad9-268">쿼리에는 자리 표시자()가`WHERE Genre = @0"`포함되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="d2ad9-269">쿼리가 변수 ()`selectCommand`에 넣습니다. 이전에는 쿼리를 `db.Query` 메서드에 직접 전달했습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="d2ad9-270">메서드를 `db.Query` 호출하면 자리 표시자에 사용할 쿼리와 값을 모두 전달합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="d2ad9-271">쿼리에 여러 자리 표시자가 있는 경우 모두 메서드에 별도의 값으로 전달합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="d2ad9-272">이러한 모든 요소를 함께 넣으면 다음 코드가 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="d2ad9-273">**중요!**</span><span class="sxs-lookup"><span data-stu-id="d2ad9-273">**Important!**</span></span> <span data-ttu-id="d2ad9-274">자리 표시자(예: `@0`)를 사용하여 SQL 명령에 값을 전달하는 것은 보안을 위해 *매우 중요합니다.*</span><span class="sxs-lookup"><span data-stu-id="d2ad9-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="d2ad9-275">변수 데이터에 대한 자리 표시자를 사용하여 여기에서 보는 방법은 SQL 명령을 생성해야 하는 유일한 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="d2ad9-276">사용자에게서 얻은 리터럴 텍스트와 값을 함께 (연결) 하여 SQL 문을 구성하지 마십시오.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="d2ad9-277">사용자 입력을 SQL 문에 연결하면 악의적인 사용자가 데이터베이스를 해킹하는 페이지에 값을 제출하는 *SQL 주입 공격이* 사이트를 엽니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="d2ad9-278">(자세한 내용은 MSDN 웹 사이트 [SQL 주입](https://msdn.microsoft.com/library/ms161953.aspx) 문서에서 확인할 수 있습니다.)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="d2ad9-279">검색 코드로 동영상 페이지 업데이트</span><span class="sxs-lookup"><span data-stu-id="d2ad9-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="d2ad9-280">이제 *Movies.cshtml* 파일에서 코드를 업데이트할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="d2ad9-281">시작하려면 페이지 상단의 코드 블록의 코드를 이 코드로 바꿉니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="d2ad9-282">여기서 차이점은 쿼리를 `selectCommand` 변수에 넣으면 `db.Query` 나중에 전달된다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="d2ad9-283">SQL 문을 변수에 넣으면 검색을 수행하기 위해 수행할 명령문을 변경할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="d2ad9-284">또한 이 두 줄을 제거했는데 나중에 다시 배치할 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="d2ad9-285">쿼리를 아직 실행하지 않으려는 경우(즉, `db.Query`호출) `WebGrid` 도우미를 초기화하고 싶지도 않습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="d2ad9-286">어떤 SQL 문을 실행해야 할지 파악한 후에 이러한 작업을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="d2ad9-287">이 다시 작성된 블록 후에 검색을 처리하기 위한 새 논리를 추가할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="d2ad9-288">완성된 코드는 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-288">The completed code will look like the following.</span></span> <span data-ttu-id="d2ad9-289">이 예제와 일치되도록 페이지의 코드를 업데이트합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="d2ad9-290">이제 이 페이지가 다음과 같이 작동합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-290">The page now works like this.</span></span> <span data-ttu-id="d2ad9-291">페이지가 실행될 때마다 코드가 데이터베이스를 열고 `selectCommand` 변수는 `Movies` 테이블에서 모든 레코드를 가져옵니다 SQL 문으로 설정됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="d2ad9-292">또한 코드는 변수를 `searchTerm` 초기화합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="d2ad9-293">그러나 현재 요청에 `searchGenre` 요소에 대한 값이 포함된 경우 `selectCommand` 코드는 장르를 검색하는 `Where` 절을 포함하는 쿼리와 같은 다른 쿼리로 설정됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="d2ad9-294">또한 검색 `searchTerm` 상자에 대해 전달된 대로 설정합니다(아무 것도 아닐 수도 있음).</span><span class="sxs-lookup"><span data-stu-id="d2ad9-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="d2ad9-295">에 있는 SQL 문에 `selectCommand`관계 없이 `db.Query` 코드는 쿼리를 실행 하기 `searchTerm`위해 호출 하 고 에 있는 모든 것을 전달 합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="d2ad9-296">`searchTerm`에 아무 것도 없는 경우 아무 것도 중요하지 `selectCommand` 않습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="d2ad9-297">마지막으로 코드는 이전과 `WebGrid` 마찬가지로 쿼리 결과를 사용하여 도우미를 초기화합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="d2ad9-298">SQL 문과 검색어를 변수에 배치하면 코드에 유연성을 추가했습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="d2ad9-299">이 자습서의 왼쪽 에서 볼 수 있듯이 이 기본 프레임 워크를 사용 하 여 다른 유형의 검색에 대 한 논리를 계속 추가할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="d2ad9-300">장르별 검색 기능 테스트</span><span class="sxs-lookup"><span data-stu-id="d2ad9-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="d2ad9-301">웹매트릭스에서 *Movies.cshtml 페이지를 실행합니다.*</span><span class="sxs-lookup"><span data-stu-id="d2ad9-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="d2ad9-302">장르에 대한 텍스트 상자가 있는 페이지가 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="d2ad9-303">테스트 레코드 중 하나에 입력한 장르를 입력한 다음 **검색을**클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="d2ad9-304">이번에는 해당 장르와 일치하는 영화 목록만 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-304">This time you see a listing of just the movies that match that genre:</span></span>

![장르 '코미디'를 검색 한 후 영화 페이지 목록](form-basics/_static/image4.png)

<span data-ttu-id="d2ad9-306">다른 장르를 입력하고 다시 검색합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-306">Enter a different genre and search again.</span></span> <span data-ttu-id="d2ad9-307">검색에 대소문자를 구분하지 않도록 모든 소문자 또는 모든 대문자문자를 사용하여 장르를 입력해 보십시오.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="d2ad9-308">사용자가 입력한 내용 "기억"</span><span class="sxs-lookup"><span data-stu-id="d2ad9-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="d2ad9-309">장르를 입력하고 **검색 장르를**클릭한 후 해당 장르에 대한 목록을 보았습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="d2ad9-310">그러나 검색 텍스트 상자는 &mdash; 비어 있었고 입력한 내용을 기억하지 못했습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="d2ad9-311">이 문제가 발생하는 이유를 이해하는 것이 중요합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="d2ad9-312">페이지를 제출하면 브라우저가 웹 서버에 요청을 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="d2ad9-313">ASP.NET 요청을 받으면 페이지의 새 인스턴스를 만들고, 페이지에서 코드를 실행한 다음 페이지를 브라우저로 다시 렌더링합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="d2ad9-314">그러나 실제로 페이지는 이전 버전의 자체 작업만 했다는 것을 알지 못합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="d2ad9-315">알고있는 것은 양식 데이터가있는 요청을 받았다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="d2ad9-316">페이지를 처음 요청할 &mdash; 때마다 또는 페이지를 제출하여 &mdash; 새 페이지를 얻게 됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="d2ad9-317">웹 서버에 마지막 요청에 대한 메모리가 없습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="d2ad9-318">어느 쪽도 ASP.NET 않으며 브라우저도 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="d2ad9-319">페이지의 이러한 개별 인스턴스 간에 만 연결되는 것은 페이지 간에 전송하는 모든 데이터입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="d2ad9-320">예를 들어 페이지를 제출하는 경우 새 페이지 인스턴스는 이전 인스턴스에서 보낸 양식 데이터를 얻을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="d2ad9-321">(페이지 간에 데이터를 전달하는 또 다른 방법은 쿠키를 사용하는 것입니다.)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="d2ad9-322">이 상황을 설명하는 공식적인 방법은 웹 페이지가 *상태 비수라고*말하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="d2ad9-323">웹 서버와 페이지 자체 및 페이지의 요소는 페이지의 이전 상태에 대한 정보를 유지 관리하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="d2ad9-324">웹은 개별 요청에 대한 상태를 유지 관리하면 초당 수천, 심지어 수십만 개의 요청을 처리하는 웹 서버의 리소스가 빠르게 소모되기 때문에 이러한 방식으로 설계되었습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="d2ad9-325">그래서 텍스트 상자가 비어 있는 이유입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-325">So that's why the text box was empty.</span></span> <span data-ttu-id="d2ad9-326">페이지를 제출한 ASP.NET 페이지의 새 인스턴스를 만들고 코드와 태그를 실행했습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="d2ad9-327">ASP.NET 텍스트 상자에 값을 넣으라고 말한 코드에는 아무것도 없었습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="d2ad9-328">그래서 ASP.NET 아무것도하지 않았고, 텍스트 상자는 값없이 렌더링되었습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="d2ad9-329">실제로이 문제를 해결할 수있는 쉬운 방법이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="d2ad9-330">텍스트 상자에 입력한 *장르는* &mdash; `Request.QueryString["searchGenre"]`의 코드에서 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="d2ad9-331">이 예제와 같이 `value` 특성에서 값을 `searchTerm`얻습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="d2ad9-332">이 페이지에서는 해당 `value` `searchTerm` 변수에 입력한 장르도 포함되어 있으므로 변수에 속성을 설정할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="d2ad9-333">그러나 여기에 `Request` 표시된 대로 `value` 속성을 설정하는 개체를 사용하는 것이 이 작업을 수행하는 표준 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="d2ad9-334">(경우에 따라 이 &mdash; 작업을 수행하려는 경우 필드에 값이 *없는* 페이지를 렌더링할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="d2ad9-335">모든 앱에서 무슨 일이 일어나고 있는지에 따라 달라집니다.)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="d2ad9-336">암호에 사용되는 텍스트 상자의 값은 "기억"할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="d2ad9-337">사람들이 코드를 사용하여 암호 필드를 채울 수 있도록 하는 보안 구멍이 될 것입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="d2ad9-338">페이지를 다시 실행하고 장르를 입력한 다음 **검색 장르를**클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="d2ad9-339">이번에는 검색 결과가 표시될 뿐만 아니라 텍스트 상자에 마지막으로 입력한 내용을 기억합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![텍스트 상자가 이전 항목을 '기억'했다는 페이지 표시](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="d2ad9-341">제목에 있는 모든 단어 검색</span><span class="sxs-lookup"><span data-stu-id="d2ad9-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="d2ad9-342">이제 모든 장르를 검색할 수 있지만 제목을 검색할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="d2ad9-343">검색할 때 제목을 정확하게 얻기가 어렵기 때문에 제목 안에 나타나는 단어를 검색할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="d2ad9-344">SQL에서 이렇게 하려면 `LIKE` 다음과 같이 연산자와 구문을 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="d2ad9-345">이 명령은 제목에 "모험"이 포함된 모든 영화를 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="d2ad9-346">연산자사용 `LIKE` 시 와일드카드 문자를 `%` 검색어의 일부로 포함합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="d2ad9-347">검색은 `LIKE 'adventure%'` '모험'으로 시작'을 의미합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="d2ad9-348">(기술적으로, 그것은 "문자열 '모험'뒤에 아무것도 를 의미합니다.) 마찬가지로 검색어는 `LIKE '%adventure'` '어드벤처'라는 문자열다음에 '모험'이라는 의미로,'모험'으로 끝나는 또 다른 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="d2ad9-349">따라서 검색어는 `LIKE '%adventure%'` "제목의 어느 곳에서나 '모험'을 의미합니다."</span><span class="sxs-lookup"><span data-stu-id="d2ad9-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="d2ad9-350">(기술적으로, "제목에 있는 모든 것, '모험', 그 다음에 무엇이든 뒤따릅니다.)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="d2ad9-351">`<form>` 요소 내부에 장르 검색을 위한 닫기 `</div>` 태그 바로 아래에 다음 태그(닫기 `</form>` 요소 바로 앞)를 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="d2ad9-352">이 검색을 처리하는 코드는 검색을 어셈블해야 한다는 점을 제외하면 장르 `LIKE` 검색의 코드와 유사합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="d2ad9-353">페이지 상단의 코드 블록 안에 장르 `if` 검색 블록 `if` 바로 바로 다음의 이 블록을 추가합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="d2ad9-354">이 코드는 검색에서 `LIKE` 연산자 및 코드는 검색 용어 앞과 후에 "를`%`넣는다는 점을 제외하고는 앞에서 본 것과 동일한 논리를 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="d2ad9-355">페이지에 다른 검색을 쉽게 추가하는 방법을 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="d2ad9-356">여러분이 해야 할 일은 다음과 같은 것이었습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-356">All you had to do was:</span></span>

- <span data-ttu-id="d2ad9-357">테스트된 `if` 블록을 만들어 관련 검색 상자에 값이 있는지 확인합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="d2ad9-358">변수를 `selectCommand` 새 SQL 문으로 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="d2ad9-359">쿼리에 `searchTerm` 전달할 값으로 변수를 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="d2ad9-360">다음은 제목 검색에 대한 새 논리를 포함하는 전체 코드 블록입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="d2ad9-361">이 코드에서 수행하는 기능을 요약하면 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="d2ad9-362">변수와 `selectCommand` `searchTerm` 맨 위에 초기화됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="d2ad9-363">이러한 변수를 페이지의 사용자에 따라 적절한 검색어(있는 경우)와 적절한 SQL 명령으로 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="d2ad9-364">기본 검색은 데이터베이스에서 모든 동영상을 가져오는 간단한 경우입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="d2ad9-365">에 대 `searchGenre` `searchTitle`한 테스트에서 코드는 검색 할 값으로 설정 `searchTerm` 합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="d2ad9-366">또한 이러한 코드 `selectCommand` 블록은 해당 검색에 적합한 SQL 명령으로 설정됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="d2ad9-367">메서드는 `db.Query` SQL `selectedCommand` 명령이 있고 `searchTerm`에 있는 값을 사용하여 한 번만 호출됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="d2ad9-368">검색어가 없는 경우(장르및 제목 단어가 없음) `searchTerm` 값은 빈 문자열입니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="d2ad9-369">그러나 이 경우 쿼리에 매개 변수가 필요하지 않기 때문에 이는 중요하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="d2ad9-370">제목 검색 기능 테스트</span><span class="sxs-lookup"><span data-stu-id="d2ad9-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="d2ad9-371">이제 완료된 검색 페이지를 테스트할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-371">Now you can test your completed search page.</span></span> <span data-ttu-id="d2ad9-372">*동영상 실행.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="d2ad9-373">장르를 입력하고 **검색 장르를 클릭합니다.**</span><span class="sxs-lookup"><span data-stu-id="d2ad9-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="d2ad9-374">그리드는 이전과 같이 해당 장르의 영화를 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="d2ad9-375">제목 단어를 입력하고 **제목 검색을**클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="d2ad9-376">표는 제목에 해당 단어가 있는 동영상을 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-376">The grid displays movies that have that word in the title.</span></span>

![제목에서 'The'를 검색한 후 동영상 페이지 목록](form-basics/_static/image6.png)

<span data-ttu-id="d2ad9-378">두 텍스트 상자를 모두 비워 두고 두 단추를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="d2ad9-379">그리드에 모든 동영상이 표시됩니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="d2ad9-380">쿼리 결합</span><span class="sxs-lookup"><span data-stu-id="d2ad9-380">Combining the Queries</span></span>

<span data-ttu-id="d2ad9-381">수행할 수 있는 검색은 배타적일 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="d2ad9-382">두 검색 상자에 값이 있는 경우에도 제목과 장르를 동시에 검색할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="d2ad9-383">예를 들어 제목에 "Adventure"가 포함된 모든 액션 영화를 검색할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="d2ad9-384">(페이지가 지금 코딩되었으므로 장르와 제목 모두에 대한 값을 입력하면 제목 검색이 우선합니다.) 조건을 결합하는 검색을 만들려면 다음과 같은 구문이 있는 SQL 쿼리를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="d2ad9-385">그리고 다음과 같은 문을 사용하여 쿼리를 실행해야합니다 (대략 말하기).</span><span class="sxs-lookup"><span data-stu-id="d2ad9-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="d2ad9-386">검색 조건의 많은 순열을 허용하는 논리를 만들면 볼 수 있듯이 약간의 관련이 있을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="d2ad9-387">따라서 여기서 멈추겠습니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="d2ad9-388">다음 단계로 올라오기</span><span class="sxs-lookup"><span data-stu-id="d2ad9-388">Coming Up Next</span></span>

<span data-ttu-id="d2ad9-389">다음 자습서에서는 사용자가 데이터베이스에 동영상을 추가할 수 있도록 폼을 사용하는 페이지를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="d2ad9-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="d2ad9-390">동영상 페이지 전체 목록(검색으로 업데이트)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="d2ad9-391">추가 리소스</span><span class="sxs-lookup"><span data-stu-id="d2ad9-391">Additional Resources</span></span>

- [<span data-ttu-id="d2ad9-392">Razor 구문을 사용하는 ASP.NET 웹 프로그래밍 소개</span><span class="sxs-lookup"><span data-stu-id="d2ad9-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="d2ad9-393">W3Schools 사이트의 [SQL WHERE 절](http://www.w3schools.com/sql/sql_where.asp)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="d2ad9-394">W3C 사이트의 [메서드 정의](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) 문서</span><span class="sxs-lookup"><span data-stu-id="d2ad9-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="d2ad9-395">[이전](displaying-data.md)
> [다음](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="d2ad9-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
